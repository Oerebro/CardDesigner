Since I have no clue how to do this in a non-hacky way within java
every font version (ie. PlantinMT Pro regular / PlantinMTPro italic) needs to have a UNIQUE 
font family name. Otherwise StyledDocument wont use the correct versions
because it only allows me to set font family and not fonts directly for WHATEVER reason.